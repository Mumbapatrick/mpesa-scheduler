spring.application.name=mpesa-scheduler

# ? Use Supabase Session Pooler (IPv4-compatible)
spring.datasource.url=jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=postgres.tfhzqfenllsemnsngfcy
spring.datasource.password=Mpesascheduler2025
spring.datasource.driver-class-name=org.postgresql.Driver

# ? Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ? HikariCP (connection pool)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

mpesa.consumerKey="${MPESA_CONSUMER_KEY}"
mpesa.consumerSecret="${MPESA_CONSUMER_SECRET}"
mpesa.shortCode="${MPESA_SHORTCODE}"
mpesa.passKey="${MPESA_PASSKEY}"
mpesa.baseUrl="${MPESA_BASE_URL}"
app.base-url="${APP_BASE_URL}"
server.port=${PORT:8080}
server.address=0.0.0.0
